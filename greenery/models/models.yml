version: 2

models:
  - name: count_users
    description: Number of users in system
    columns:
      - name: number_of_users
        description: Number of users
        tests:
          - not_null
          - unique
